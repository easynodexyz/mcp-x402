---
title: "Cursor"
description: "Configure MCP x402 with Cursor IDE"
---

# Cursor

## Remote Server

Add to your Cursor MCP settings (`.cursor/mcp.json`):

```json
{
  "mcpServers": {
    "easy-node": {
      "url": "https://mcp-x402.easy-node.xyz/mcp",
      "headers": {
        "X-Easynode-Private-Key": "0x..."
      }
    }
  }
}
```

## Local Server

```json
{
  "mcpServers": {
    "easy-node": {
      "command": "npx",
      "args": ["-y", "@easynodexyz/mcp-x402"]
    }
  }
}
```

## Config File Locations

| Scope | Path |
|-------|------|
| Project | `.cursor/mcp.json` in project root |
| Global (macOS) | `~/.cursor/mcp.json` |
| Global (Windows) | `%USERPROFILE%\.cursor\mcp.json` |

## After Configuration

Restart Cursor to load the MCP server.
