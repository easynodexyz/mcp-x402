startCommand:
  type: http
  configSchema:
    type: object
    properties:
      EASYNODE_PRIVATE_KEY:
        type: string
        description: 'Wallet private key (0x...) — sent via X-Easynode-Private-Key header'
    required:
      - EASYNODE_PRIVATE_KEY
